My first readme
I've been asked to update this but it appears I do not have much to say other that list the few commands I have so far executed. They include:
git add .
git commit -m 'commit message'
git push <remote> <branchname>
git --set-upstream <remote> <branchname>
  
